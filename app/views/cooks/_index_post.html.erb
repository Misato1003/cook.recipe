<h3>レビュー一覧表</h3>
<table>
  <thead>
    <tr>
      <th>投稿者の名前</th>
      <th>レビューした日付</th>
      <th>レビュー内容</th>
      <th>投稿者（User _id)</th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.nickname %></td>
        <td><%= post.created_at.to_s(:datetime_jp) %></td>
        <td><%= post.content %></td>
        <td><%= post.user_id %></td>
      </tr>
    <% end %>
  </tbody>
</table>